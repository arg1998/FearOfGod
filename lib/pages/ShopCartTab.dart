import 'package:flutter/material.dart';


class ShopCartTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("SHOP CARD TAB"),
      ),
    );
  }
}